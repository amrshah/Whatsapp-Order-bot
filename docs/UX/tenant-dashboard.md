1. Should have pre-defined categories from which they can select deselect default ones in addition to Add category option
2. MUST have support for multiple-branches as many restaurants/fastfood setups have multibranch network
3. if raw-materials/components required for an item are low/unavailable there should be an option for the manager to notify customer that they should select another item; a customer should see in the menu if an item is unavailable (this will need inventory system)
4. also add multi user/roles for the tenants so that they can create staff accounts (cooks, manager, rider etc)


